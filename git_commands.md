### Saving a Snapshot 

```
git add -A
git commit -m "your descriptive commit message"
```


### Pushing to Github
```
git push origin master
```

### Pushhing to Heroku 
```
git push heroku master
```
